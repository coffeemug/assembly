[project]
name = "assembly"
version = "0.1.0"
description = "Election game MVP: two AI representatives negotiate for polarized human factions."
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0,<2",
    "matplotlib>=3.9,<4",
    "scipy>=1.10,<2",
]

[tool.uv]
package = false
